#!/bin/bash

# ============================================
# 博客平台快捷命令
# 使用方法：blog 或 blog <数字>
# ============================================

# 脚本所在目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 执行部署脚本
exec "$SCRIPT_DIR/deploy.sh" "$@"
